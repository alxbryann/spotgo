import {getCurrentCompanyId} from "@/lib/company";import {createClient} from "@/lib/supabase/server";import LotForm from "@/components/dashboard/LotForm";
export default async function Page(){const s=await createClient();const companyId=await getCurrentCompanyId(s);const {data:lots}=companyId?await s.from("parking_lots").select("*").eq("company_id",companyId).order("name").limit(1): {data:[]};return <><p className="font-bold text-lime-700">Configuración</p><h1 className="mt-1 text-4xl font-black">Tu parqueadero</h1><p className="mt-2 text-slate-600">Actualiza la información que ven los conductores.</p><div className="mt-7"><LotForm lot={lots?.[0]}/></div></>}
