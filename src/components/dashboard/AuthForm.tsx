"use client"; import { useState } from "react"; import { signIn,signUp } from "@/app/actions/auth";
export default function AuthForm({register=false}:{register?:boolean}){const [error,setError]=useState("");async function submit(f:FormData){setError("");const r=register?await signUp({name:String(f.get("name")),nit:String(f.get("nit")),phone:String(f.get("phone")),email:String(f.get("email")),password:String(f.get("password"))}):await signIn({email:String(f.get("email")),password:String(f.get("password"))});if(r?.error)setError(r.error)}return <form action={submit} className="space-y-4 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">{register&&<><label className="block text-sm font-bold">Empresa<input name="name" required className="mt-1 w-full rounded-xl border p-3" /></label><label className="block text-sm font-bold">NIT<input name="nit" className="mt-1 w-full rounded-xl border p-3" /></label><label className="block text-sm font-bold">Teléfono<input name="phone" className="mt-1 w-full rounded-xl border p-3" /></label></>}<label className="block text-sm font-bold">Correo<input name="email" type="email" required className="mt-1 w-full rounded-xl border p-3" /></label><label className="block text-sm font-bold">Contraseña<input name="password" type="password" minLength={8} required className="mt-1 w-full rounded-xl border p-3" /></label>{error&&<p role="alert" className="text-sm font-semibold text-red-700">{error}</p>}<button className="min-h-11 w-full rounded-xl bg-lime-500 px-4 font-black text-slate-950 hover:bg-lime-400">{register?"Crear empresa":"Entrar al panel"}</button></form>}
